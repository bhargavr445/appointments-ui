# build cmd
ng build --configuration production --output-path docs --base-href "/" --prerender=false

# above command will generate following folder  `docs -> browser`, make sure to move all the [files/folders] from `browser` folder to under `docs` folder`

# git cmd
git pull origin zone-less-20.2


# open this link 
https://github.com/bhargavr445/appointments-ui/settings/pages

# check which branch is source branch 
currently - `zone-less-20.2` branch is source



# paste below custom domain and save. 
czjunkremoval.com